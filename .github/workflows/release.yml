name: Release

on:
  push:
    tags:
      - '*.*.*'

jobs:
  release:
    name: "Release"
    runs-on: "ubuntu-latest"

    steps:
        uses: softprops/action-gh-release@v1
        with:
          files: "*.ps1"
